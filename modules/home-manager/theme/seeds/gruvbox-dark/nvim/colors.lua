-- gruvbox-dark colorscheme. Lives at ~/.config/nvim/lua/colors/<theme>.lua
-- via a Nix-managed symlink so `:colorscheme gruvbox-dark` resolves here.

vim.o.background = "dark"
vim.cmd("colorscheme gruvbox")